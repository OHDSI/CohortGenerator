# Used to check if a string is in snake case

This function is used check if a string conforms to the snake case
format.

## Usage

``` r
isSnakeCase(x)
```

## Arguments

- x:

  The string to evaluate

## Value

TRUE if the string is in snake case
