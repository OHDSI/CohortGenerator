# Is the data.frame a cohort definition set?

This function checks a data.frame to verify it holds the expected format
for a cohortDefinitionSet.

## Usage

``` r
isCohortDefinitionSet(x)
```

## Arguments

- x:

  The data.frame to check

## Value

Returns TRUE if the input is a cohortDefinitionSet or returns FALSE with
warnings on any violations
